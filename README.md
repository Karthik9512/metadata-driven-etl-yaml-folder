# Metadata-Driven ETL Pipeline Using YAML (Folder-Based Source)
Build a reusable, metadata-driven ETL framework in Databricks where new datasets can be onboarded simply by:  
-> Adding a new CSV file to the source folder (Volume).
-> Updating the YAML configuration with the dataset details. 
-> Running the ETL pipeline without making any code changes.
